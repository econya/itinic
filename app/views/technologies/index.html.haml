%h1 Listing Technologies

%table.table
  %thead
    %tr
      %th Name
      %th Kind
      %th People (level/assessment)
  %tbody
    = render partial: 'tr_technology', collection: @technologies || content_tag(:tr, content_tag(:td, 'No technologies'))

= add_button "Technology"
