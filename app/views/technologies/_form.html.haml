= simple_form_for @technology, html: { class: 'form-horizontal' }, wrapper: :horizontal_form do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :kind, collection: enum_map(Technology.kinds)

  %br

  .form-actions
    = f.button :submit
